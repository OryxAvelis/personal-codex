{
  "name": "personal-codex-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A private localhost interface for Codex.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check launcher.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@phosphor-icons/web": "^2.1.2"
  }
}
